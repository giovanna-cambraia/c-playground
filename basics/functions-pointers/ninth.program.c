#include <stdio.h>

/* void change (int *x) {
    *x = *x * 2;
}

int main() {
    int num = 10;

    change(&num);

    printf("%d\n", num);
    return 0;
} */

/* void swap(int *a, int *b)
{
    int temp = *a;
    *a = *b;
    *b = temp;
}

int main() {
    int x = 67, y = 63;

    swap(&x, &y);

    printf("x = %d, y = %d\n", x, y);

    return 0;
} */

