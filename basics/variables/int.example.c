#include <stdio.h>

int main() {
    int age = 17;

    printf("Age: %d\n", age);
    return 0;
}

// %d is a format specifier used in printf to indicate that the corresponding argument is an integer. 
// In this case, it will print the value of the variable 'age' which is 17.