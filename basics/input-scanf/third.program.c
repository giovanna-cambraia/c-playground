#include <stdio.h>

/* int main() {
    int age;

    printf("How old are you? ");
    scanf("%d", &age);

    printf("You are %d years old.\n", age);
    return 0;
} */

/* int main () {
    int year;
    float temperature;

    printf("Enter the year: ");
    scanf("%d", &year);

    printf("Enter the temperature: ");
    scanf("%f", &temperature);

    printf("Year: %d\nTemperature: %.2f\n", year, temperature);

    return 0;
} */

#include <stdio.h>

int main() {
    int age;
    float height;

    printf("Enter age: ");
    scanf("%d", &age);

    printf("Enter height: ");
    scanf("%f", &height);

    printf("Age: %d\n", age);
    printf("Height: %.2f\n", height);

    return 0;
}